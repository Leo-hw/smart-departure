# T-011-report — 당일 추가 일정 스냅샷 staleness 핫픽스

**완료일**: 2026-06-07
**담당**: Codex

---

## AC 결과

| AC | 결과 | 비고 |
|----|------|------|
| 당일 추가 일정을 다음 실행에 반영 | ✅ | 기본 TTL 0에서 매 실행 계획 재빌드 |
| 날짜-only 스냅샷 재사용 제거 | ✅ | `built_at`과 TTL을 함께 검사 |
| dedup 상태 영속화 유지 | ✅ | 재빌드 후 기존 알림이 중복으로 필터되는 테스트 추가 |
| Maps 캐시 정책 유지 | ✅ | `maps_service.py` 미변경, 기존 6시간 캐시 테스트 통과 |
| 선택적 스냅샷 TTL 설정 | ✅ | `snapshot_ttl_minutes: 0` 기본값 추가 |
| 스냅샷 staleness 단위 테스트 | ✅ | 빈 오늘 스냅샷 대신 새 계획 재빌드 검증 |

## 원래 기획과 달라진 점

- 없음

## 발생한 에러/이슈

- 기존 스냅샷에는 `built_at`이 없으므로 TTL을 양수로 설정해도 최초 실행에서는 한 번 재빌드됨

## 다음 작업자에게 전달할 주의사항

- T-009에서 스냅샷 포맷을 확장할 때 `date`, `built_at`, `plans` 구조를 유지해야 함
- 기본값 0은 Calendar를 매 실행 조회하지만 이동 시간은 기존 Maps 캐시를 계속 사용함
